# On Vercel, leave this EMPTY — the API is on the same domain (serverless functions)
# Only set this if you host the backend separately (e.g. Render.com)
# Example: VITE_API_URL=https://lsafe-api.onrender.com
VITE_API_URL=
